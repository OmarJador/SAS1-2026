const prompt = require("prompt-sync")();

let day = parseInt(prompt("Tapez un jour (de 1 a 31) :"));
let month = parseInt(prompt("Tapez un mois (de 1 a 12) :"));
let year = parseInt(prompt("Tapez une annee (de 1 a 2026) :"));

